"use client";

export const runtime = 'edge';
import {Container} from '@mui/material';

export default function Page() {
  return (
    <Container>
      <p>route not found :(</p>
    </Container>
  );
};